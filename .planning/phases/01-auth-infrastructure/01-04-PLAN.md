---
phase: 1
plan: 04
title: "Saxo API client with rate limiting, retries, and error normalization"
wave: 2
depends_on: [01, 02]
requirements: [INFRA-04, INFRA-05]
files_modified:
  - backend/services/saxo_client.py
autonomous: true
---

<objective>
Build the SaxoClient class — an httpx.AsyncClient wrapper that handles automatic Bearer token injection, rate limit header parsing (X-RateLimit-Remaining, Retry-After), exponential backoff with jitter on 429/503/timeout responses using tenacity, 10-second request timeout, and normalization of all Saxo error responses into typed exceptions from the exception hierarchy.
</objective>

<tasks>

<task id="04.1">
<title>Create SaxoClient with token injection, retries, and error normalization</title>
<read_first>
- backend/services/data_fetcher.py
- backend/services/saxo_exceptions.py
- backend/config.py
</read_first>
<action>
Create file `backend/services/saxo_client.py` with a class `SaxoClient`:

```python
import logging
from typing import Any, Dict, Optional

import httpx
from tenacity import (
    retry,
    stop_after_attempt,
    wait_exponential_jitter,
    retry_if_exception,
)

from config import SAXO_BASE_URL
from services.saxo_exceptions import (
    SaxoAPIError,
    SaxoAuthError,
    SaxoRateLimitError,
    SaxoError,
)

logger = logging.getLogger(__name__)


def _is_retryable(exc: Exception) -> bool:
    """Determine if an exception is retryable."""
    if isinstance(exc, SaxoRateLimitError):
        return True
    if isinstance(exc, httpx.TimeoutException):
        return True
    if isinstance(exc, httpx.HTTPStatusError):
        return exc.response.status_code in (429, 502, 503)
    return False


class SaxoClient:
    """HTTP client wrapper for Saxo OpenAPI with automatic token injection,
    rate limit handling, retries, and error normalization.

    Usage:
        client = SaxoClient(http_client)
        data = await client.get(access_token, "/port/v1/clients/me")
    """

    def __init__(self, http_client: httpx.AsyncClient):
        self._http = http_client
        self._base_url = SAXO_BASE_URL

    @retry(
        retry=retry_if_exception(_is_retryable),
        stop=stop_after_attempt(3),
        wait=wait_exponential_jitter(initial=1, max=10),
    )
    async def get(
        self,
        access_token: str,
        path: str,
        params: Optional[Dict[str, Any]] = None,
    ) -> Dict[str, Any]:
        """Make a GET request to Saxo OpenAPI.

        Args:
            access_token: Decrypted Bearer token (plaintext, in-memory only).
            path: API path (e.g., "/port/v1/clients/me").
            params: Optional query parameters.

        Returns:
            Parsed JSON response as dict.

        Raises:
            SaxoAuthError: On 401 response.
            SaxoRateLimitError: On 429 response (will be retried by tenacity).
            SaxoAPIError: On other non-2xx responses.
        """
        url = f"{self._base_url}{path}"
        headers = {
            "Authorization": f"Bearer {access_token}",
            "Accept": "application/json",
        }

        response = await self._http.get(
            url,
            headers=headers,
            params=params,
            timeout=10.0,
        )

        return self._handle_response(response)

    @retry(
        retry=retry_if_exception(_is_retryable),
        stop=stop_after_attempt(3),
        wait=wait_exponential_jitter(initial=1, max=10),
    )
    async def post(
        self,
        access_token: str,
        path: str,
        json_body: Optional[Dict[str, Any]] = None,
    ) -> Dict[str, Any]:
        """Make a POST request to Saxo OpenAPI."""
        url = f"{self._base_url}{path}"
        headers = {
            "Authorization": f"Bearer {access_token}",
            "Accept": "application/json",
            "Content-Type": "application/json",
        }

        response = await self._http.post(
            url,
            headers=headers,
            json=json_body,
            timeout=10.0,
        )

        return self._handle_response(response)

    def _handle_response(self, response: httpx.Response) -> Dict[str, Any]:
        """Parse response, log rate limit headers, raise typed exceptions on errors."""
        # Log rate limit headers (never log tokens)
        remaining = response.headers.get("X-RateLimit-Remaining")
        if remaining is not None:
            logger.debug("Saxo rate limit remaining: %s", remaining)

        if response.status_code == 200:
            if not response.content:
                return {}
            return response.json()

        if response.status_code == 204:
            return {}

        if response.status_code == 401:
            raise SaxoAuthError("Saxo returned 401 Unauthorized")

        if response.status_code == 429:
            retry_after = int(response.headers.get("Retry-After", "1"))
            logger.warning("Saxo rate limit hit. Retry-After: %ds", retry_after)
            raise SaxoRateLimitError(retry_after)

        # Parse Saxo error body — two possible shapes:
        # API errors: {"ErrorCode": "...", "Message": "..."}
        # OAuth errors: {"error": "...", "error_description": "..."}
        try:
            body = response.json()
        except Exception:
            raise SaxoAPIError(
                error_code="UnparsableResponse",
                message=response.text[:200],
                status_code=response.status_code,
            )

        error_code = body.get("ErrorCode", body.get("error", "Unknown"))
        message = body.get("Message", body.get("error_description", "No details"))

        raise SaxoAPIError(
            error_code=str(error_code),
            message=str(message),
            status_code=response.status_code,
        )
```

Key design points:
- The `http_client` is injected via constructor (created in main.py lifespan, shared across services)
- `access_token` is passed per-call (obtained from `SaxoTokenService.get_valid_token()` by the router)
- tenacity retries on 429, 502, 503, and timeout — up to 3 attempts with exponential backoff + jitter
- `_handle_response` parses both Saxo error shapes (API vs OAuth)
- Rate limit headers are logged at DEBUG level
- Tokens are never logged — only the `remaining` count
- 10-second timeout on all requests
</action>
<acceptance_criteria>
- File `backend/services/saxo_client.py` exists
- File contains `class SaxoClient:`
- File contains `def __init__(self, http_client: httpx.AsyncClient):`
- File contains `async def get(self, access_token: str, path: str`
- File contains `async def post(self, access_token: str, path: str`
- File contains `def _handle_response(self, response: httpx.Response)`
- File contains `timeout=10.0`
- File contains `@retry(`
- File contains `stop_after_attempt(3)`
- File contains `wait_exponential_jitter(initial=1, max=10)`
- File contains `def _is_retryable(exc: Exception) -> bool:`
- File contains `SaxoRateLimitError`
- File contains `SaxoAuthError`
- File contains `SaxoAPIError`
- File contains `X-RateLimit-Remaining`
- File contains `Retry-After`
- File does NOT contain any log statement that outputs `access_token` value
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `grep "class SaxoClient" backend/services/saxo_client.py` — class exists
2. `grep "retry" backend/services/saxo_client.py` — tenacity retry decorator present
3. `grep "timeout=10.0" backend/services/saxo_client.py` — 10-second timeout set
4. `grep "X-RateLimit-Remaining" backend/services/saxo_client.py` — rate limit headers parsed
5. `grep "Retry-After" backend/services/saxo_client.py` — retry-after header handled
6. `python -c "from services.saxo_client import SaxoClient; print('import OK')"` (from backend dir)
</verification>

<must_haves>
- httpx.AsyncClient injected, not created internally (shared lifecycle via lifespan)
- Bearer token injected into Authorization header on every request
- 10-second timeout on all Saxo API calls
- Tenacity retry: 3 attempts, exponential backoff with jitter, on 429/502/503/timeout
- Retry-After header parsed from 429 responses
- X-RateLimit-Remaining header logged at DEBUG level
- Both Saxo error response shapes handled (ErrorCode/Message and error/error_description)
- All errors normalized to SaxoAuthError, SaxoRateLimitError, or SaxoAPIError
- Tokens never logged
</must_haves>
