---
phase: 4
plan: 01
title: "Record data source decision in PROJECT.md"
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
requirements: [DATA-01, DATA-02]
autonomous: true
---

# Plan 04-01: Record data source decision in PROJECT.md

<objective>
Close the pending "Replace Yahoo Finance with Saxo as primary data source" decision by updating PROJECT.md with a research-backed rationale, a per-feature data source assignment, and moving the evaluation requirement from Active to Validated.
</objective>

<tasks>

<task id="1">
<title>Update Key Decisions table: close the pending data source row</title>
<read_first>
- .planning/PROJECT.md
- .planning/phases/04-data-source-decision/04-RESEARCH.md
</read_first>
<action>
In `.planning/PROJECT.md`, replace the existing pending Key Decisions row:

```
| Replace Yahoo Finance with Saxo as primary data source | User wants single data source from their actual broker; Yahoo Finance to be removed | — Pending |
```

With two closed rows (Option B from research — explicit per-domain split):

```
| Data source for discovery and market features (v1.1) | Saxo has no screener capability (`/ref/v1/instruments` is reference-only), no sector performance aggregates, no most-active endpoint, and instrument coverage is account-dependent — unsuitable for broad market discovery. yfinance provides `yf.EquityQuery`, `yf.screen('most_actives')`, and `yf.Sector` with full GICS coverage. | **Closed — yfinance.** yfinance is authoritative for screener, most-traded, and sector performance features. Saxo OHLCV migration for TA signals deferred to v1.2 investigation. |
| Data source for portfolio and account data | Saxo is the only source for real positions, balances, and real-time quotes for held instruments. yfinance cannot replicate brokerage account data. | **Closed — Saxo OpenAPI.** Saxo is authoritative for all portfolio and account data (implemented v1.0). |
```

This replaces 1 row with 2 rows. All other rows in the Key Decisions table remain unchanged.
</action>
<acceptance_criteria>
- `.planning/PROJECT.md` contains the string `Data source for discovery and market features (v1.1)`
- `.planning/PROJECT.md` contains the string `Data source for portfolio and account data`
- `.planning/PROJECT.md` contains the string `Closed — yfinance.`
- `.planning/PROJECT.md` contains the string `Closed — Saxo OpenAPI.`
- `.planning/PROJECT.md` contains the string `Saxo OHLCV migration for TA signals deferred to v1.2 investigation`
- `.planning/PROJECT.md` does NOT contain the string `— Pending`
- `.planning/PROJECT.md` does NOT contain the string `User wants single data source from their actual broker`
</acceptance_criteria>
</task>

<task id="2">
<title>Move "Evaluate Saxo API vs Yahoo Finance" from Active to Validated</title>
<read_first>
- .planning/PROJECT.md
</read_first>
<action>
In `.planning/PROJECT.md`, under the `### Active` section, remove this line:

```
- [ ] Evaluate Saxo API vs Yahoo Finance as primary data source
```

Under the `### Validated` section, after the last `- ✓` line (currently `- ✓ Apply existing technical analysis signals to Saxo-held instruments — v1.0 Phase 3`), add:

```
- ✓ Evaluate Saxo API vs Yahoo Finance as primary data source — v1.1 Phase 4
```

The Active section should then start with `- [ ] Stock screener with sector/industry/market-cap browsing`.
</action>
<acceptance_criteria>
- `.planning/PROJECT.md` contains the string `✓ Evaluate Saxo API vs Yahoo Finance as primary data source — v1.1 Phase 4`
- The string `✓ Evaluate Saxo API vs Yahoo Finance` appears in the file AFTER the string `### Validated` and BEFORE the string `### Active`
- The `### Active` section does NOT contain the string `Evaluate Saxo API vs Yahoo Finance`
</acceptance_criteria>
</task>

<task id="3">
<title>Add capability comparison table to PROJECT.md</title>
<read_first>
- .planning/PROJECT.md
- .planning/phases/04-data-source-decision/04-RESEARCH.md
</read_first>
<action>
In `.planning/PROJECT.md`, immediately ABOVE the `## Context` section (line 62), insert a new section that documents the per-feature data source assignment (satisfying DATA-01's requirement for a visible comparison):

```markdown
## Data Source Assignment (v1.1)

Decided in Phase 4 based on v1.1 research. See `.planning/phases/04-data-source-decision/04-RESEARCH.md` for full capability comparison tables.

| Feature | Data Source | Key API |
|---------|------------|---------|
| Stock screener (sector/industry) | yfinance | `yf.EquityQuery`, `yf.screen()` |
| Most-traded stocks | yfinance | `yf.screen('most_actives')` |
| Sector performance (daily + YTD) | yfinance | `yf.Sector(key).overview` |
| Sector drill-down (top companies) | yfinance | `yf.Industry(key).top_performing_companies` |
| Most-viewed stocks | Supabase | `stock_view_counts` table (internal) |
| Portfolio positions & balances | Saxo OpenAPI | `/port/v1/positions`, `/port/v1/balances` |
| Real-time quotes (held instruments) | Saxo OpenAPI | `/trade/v1/infoprices` |
| Historical OHLCV for TA signals | yfinance (status quo) | `ticker.history()` |

```

This table gives any reader an unambiguous per-feature data source lookup without needing to read research files.
</action>
<acceptance_criteria>
- `.planning/PROJECT.md` contains the string `## Data Source Assignment (v1.1)`
- `.planning/PROJECT.md` contains the string `| Stock screener (sector/industry) | yfinance |`
- `.planning/PROJECT.md` contains the string `| Most-traded stocks | yfinance |`
- `.planning/PROJECT.md` contains the string `| Sector performance (daily + YTD) | yfinance |`
- `.planning/PROJECT.md` contains the string `| Portfolio positions & balances | Saxo OpenAPI |`
- `.planning/PROJECT.md` contains the string `| Most-viewed stocks | Supabase |`
- `.planning/PROJECT.md` contains the string `| Historical OHLCV for TA signals | yfinance (status quo) |`
- The string `## Data Source Assignment` appears BEFORE the string `## Context`
</acceptance_criteria>
</task>

<task id="4">
<title>Update PROJECT.md timestamp</title>
<read_first>
- .planning/PROJECT.md
</read_first>
<action>
In `.planning/PROJECT.md`, replace the line:

```
Last updated: 2026-04-05 — Milestone v1.1 started (Stock Discovery & Market Trends)
```

With:

```
Last updated: 2026-04-05 — Phase 4 complete: data source decision closed (yfinance for discovery, Saxo for portfolio)
```

Also replace the final line:

```
*Last updated: 2026-03-29 after Phase 1 completion — auth infrastructure built, pending human verification with Saxo SIM credentials*
```

With:

```
*Last updated: 2026-04-05 — Phase 4: data source decision recorded*
```
</action>
<acceptance_criteria>
- `.planning/PROJECT.md` contains the string `Phase 4 complete: data source decision closed`
- `.planning/PROJECT.md` contains the string `*Last updated: 2026-04-05 — Phase 4: data source decision recorded*`
- `.planning/PROJECT.md` does NOT contain the string `Last updated: 2026-03-29`
</acceptance_criteria>
</task>

</tasks>

<verification>
1. Run `grep "Pending" .planning/PROJECT.md` — should return zero matches (no pending decisions remain).
2. Run `grep "Closed —" .planning/PROJECT.md` — should return exactly 2 matches (one for yfinance, one for Saxo OpenAPI).
3. Run `grep "Data Source Assignment" .planning/PROJECT.md` — should return 1 match confirming the comparison table section exists.
4. Run `grep "✓ Evaluate Saxo API vs Yahoo Finance" .planning/PROJECT.md` — should return 1 match in the Validated section.
5. Run `grep "Evaluate Saxo API" .planning/PROJECT.md` — should return exactly 1 match total (only in Validated, not in Active).
6. Read the Key Decisions table and confirm it has 10 rows total (the original 8 non-pending rows plus 2 new closed rows, minus the 1 removed pending row = 9... actually 8 original + 2 new - 1 removed = 9 rows).
</verification>

<must_haves>
- The pending "Replace Yahoo Finance with Saxo as primary data source" decision is closed with specific rationale citing missing Saxo capabilities (screener, sector aggregates, most-active endpoint)
- Every v1.1 feature has an unambiguous data source assignment visible in PROJECT.md
- The Saxo OHLCV migration is explicitly deferred to v1.2
- The "Evaluate Saxo API vs Yahoo Finance" requirement is moved from Active to Validated
- No new files are created — all changes are in PROJECT.md
</must_haves>
